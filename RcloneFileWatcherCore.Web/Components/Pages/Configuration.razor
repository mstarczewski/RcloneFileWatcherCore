@page "/config"

<PageTitle>Konfiguracja — RcloneFileWatcher</PageTitle>

<h1>Konfiguracja</h1>

<div class="placeholder">
    <p>Edycja parametrów aplikacji (ścieżki, interwały, harmonogram, auto-update, logowanie)
       z zapisem do <code>RcloneFileWatcherCoreConfig.cfg</code> i przeładowaniem bez restartu
       pojawi się w <strong>Fazie 2</strong>.</p>
</div>
