@page "/rclone"

<PageTitle>Rclone — RcloneFileWatcher</PageTitle>

<h1>Rclone</h1>

<div class="placeholder">
    <p>Kreator komendy rclone (tryb „managed”: budowanie wywołania z pól formularza, z
       automatycznym <code>--include-from</code>) oraz podgląd generowanej linii poleceń
       pojawią się w <strong>Fazie 3</strong>. Obsługa istniejących skryptów
       <code>.bat</code>/<code>.sh</code> zostanie zachowana.</p>
</div>
